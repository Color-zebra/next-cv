import { WidthContainer } from "@/shared/ui/WidthContainer";
import s from "./Contacts.module.scss";

export const Contacts = () => (
  <WidthContainer id="contacts" className={s.wrapper}>
    Contacts
  </WidthContainer>
);
